# 1.6.1 (13) iPhone upgrade verification

Date: 2026-09-10. Exact candidate: `7f7134fa529109c8b39bab0f4b14b3485d25a2ca`.

- User confirmed a backup before installation and approved removing unused cloud capabilities for this local-only release.
- Independent read-only review: no Critical/Important findings. Minor future coverage suggestion: test each forbidden APS spelling independently for main products; current exact allowlists reject both.
- Development-signed Debug build: PASS, exit 0, 65.484 seconds. Existing profiles used; no Apple portal changes.
- Verified main app, embedded Watch and Share signatures with `codesign --verify --strict`; all report 1.6.1 (13), embed the exact candidate revision and have no CloudKit/APS permissions. Team and main/Share App Group checked. This is not distribution signing or archive verification.
- iPhone 17 Pro Max on iOS 26.6.1: installed version before upgrade 1.6.0 (12). Overlay installation succeeded, exit 0, and fresh device query reports 1.6.1 (13). No uninstall/reset or backup import performed.
- App launch NOT verified: an initial empty environment option was rejected by devicectl before launch. Retried with inherited DEVICECTL_CHILD_ variables removed and no app arguments; iOS denied launch because the phone was locked. No runtime crash was observed or inferred.
- Pending user verification after unlocking: launch, existing projects/patterns/yarn/counters and photos, Settings → About → story and separate usage section, relaunch persistence. No physical data-preservation or visual acceptance claimed yet.

No direct Watch or iPad installation, signed distribution archive, push, upload or submission was performed. The app embeds the Watch product; paired Watch runtime acceptance remains separate.

## Subsequent user acceptance

On 2026-09-10 the user answered 「結果正常」 to the requested iPhone checks: open KnitNote after unlocking, confirm existing projects/patterns/yarn/counter data, and check Settings → About → development story. Record these as user-reported PASS on the installed 1.6.1 (13) candidate above. This supersedes the pending launch/data/story checks, not the historical locked-device launch result.

The response does not explicitly verify force-quit/relaunch persistence, every photo, every localization, Watch operation, iPad/Mac layout, or the full upgrade regression matrix. Those remain separate from this scoped acceptance. No new code change, install, signing, push, upload or submission was performed while recording the response.

## Subsequent release checks

The old commercial integration-test failure was reproduced and corrected in test expectations only: production audit deliberately rejects KNITNOTE_COMMERCIAL_CONFIGURATION overrides before reading any supplied fixture. The renamed test now checks that real protection. The existing direct validator test continues to reject paid download configuration; no production guard or commercial setting changed. Commercial tests: 22 PASS. Metadata tests: 30 PASS via module invocation (direct-file invocation failed package import resolution). Production static audit: PASS.

Live App Store Connect check reached the login page, not an authenticated app/version page. Build 13 availability, selected build, current pricing/IAP and submission status are therefore unverified. No portal edits or upload occurred.

Bounded regression filter `JSONProjectStore|KnitNoteBackup|StoreBackup|StorePatternSessionDrain|StoreMediaSessionDrain|StoreBackgroundDrain|AppSession|PhoneWatch` ran 477 tests in 19 suites. Initial execution also reported an incompatible XCTest launcher architecture; this Core suite contains Swift Testing tests. Native arm64 rerun with `--disable-xctest` reproduced one actual stale test expectation (145.237 seconds, exit 1): selected yarn deletion expected one retained file although BackupFixture.writeCompleteArchive creates both a yarn photo and a label photo.

Corrected that expectation to two files and added checks for both media directories, restored yarn photo metadata, and SHA-256 equality of each restored file. No production implementation was changed. Fresh `JSONProjectStoreSyncDeletionTests`: all 37 tests PASS, 13.277 seconds, exit 0. The complete 477-test filter has not been rerun after this test-only correction; do not label the earlier run all-green. Other platform acceptance, full release regression, formal archives and live store checks remain outstanding.
