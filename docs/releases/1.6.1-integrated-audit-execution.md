# 1.6.1 (13) integrated audit execution

Date: 2026-09-11. Predecessor: `3fbe7d5d1deeb665ab9a80e4313284c84f771fcb`.

## Narrow execution correction

The production archive audit now explicitly invokes
`swift test --disable-sandbox --no-parallel`. It still invokes the whole package
test suite, without filters, skips, disabled test frameworks or reused evidence.
The existing shell failure propagation remains unchanged.

Prior parallel-run sharing timeouts did not recur during sequential batches;
those observations justify serial execution but do not prove every previous
timeout had the same cause. Earlier inventory coverage across batches is not
being substituted for this new integrated run.

Regression evidence:

- RED: the new invocation test observed the real audit calling only `test` and
  `--disable-sandbox`, missing the explicit `--no-parallel` parameter.
- GREEN: both new tests passed in 8.044 seconds. They verify the invocation at
  the Swift process boundary and that a test-process failure exits with status
  42 before product extraction or an audit PASS.
- Shell syntax and production static audit passed.

## Frozen-candidate workflow

1. Commit only this audit correction, its tests and this record.
2. Create a clean local clone at that exact commit, preserving unrelated notes
   in the development worktree and all historical candidates.
3. Run the unmodified production `create_release_candidate.sh` from that clone,
   with source/build/output staging outside the Documents file-provider area.
   Local export only; no upload or version auto-management.
4. Let its complete test stage finish, then require the same script's full
   exported-product/provenance checks and atomic local publication to succeed.
5. Preserve the final log and a verified backup outside the source checkout.

The exported candidate's embedded source SHA must equal the source and validator
commit throughout this run. No further source edits during generation. A result
record must live outside the frozen checkout so documenting results does not
invalidate its identity.

Completion of this local workflow is not physical acceptance, a Git push,
App Store upload, review submission or release approval. Those remaining gates
must still be reported independently. No complete integrated result is claimed
by this pre-execution document.
