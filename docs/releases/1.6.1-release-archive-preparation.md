# 1.6.1 (13) release archive preparation

Date: 2026-09-10. Source candidate: `0dd2e906f5a1726d31b67a39b83c3f5afdbb5dd6`.

## Verified before archiving

- Native arm64 Swift Testing regression filter `JSONProjectStore|KnitNoteBackup|StoreBackup|StorePatternSessionDrain|StoreMediaSessionDrain|StoreBackgroundDrain|AppSession|PhoneWatch`: 477 tests in 19 suites PASS, 148.556 seconds, exit 0. This is the full previously selected regression batch, not every repository test.
- Actual app source/configuration is unchanged from user-accepted iPhone candidate `7f7134fa529109c8b39bab0f4b14b3485d25a2ca`; the intervening commit changes two test files and the acceptance record only.
- Required local Apple Distribution identity is present. No certificate/private-key material exported.
- Clean local clone at `/tmp/knitnote-161-release-VtfdTt/source`, detached at the exact candidate. Original worktree's unrelated untracked notes preserved. Static production audit PASS in both original worktree and clean clone.
- ExportOptions-AppStore.plist requests local export, not upload, and disables automatic version/build changes. No export performed yet.

## Live store observation from prior authenticated check

The iOS/macOS version menu displayed only 1.6.0; each platform's TestFlight latest visible build was 12. No 1.6.1 draft or build 13 was seen. iOS upload history's newest visible entry was completed 1.6.0 (12). These are time-bounded UI observations, not proof that a future upload will be accepted. Current commercial settings were not yet checked.

## Local archive results

- iOS Release archive PASS, exit 0, 308.308 seconds: `/tmp/knitnote-161-release-VtfdTt/KnitNote-iOS.xcarchive`.
- macOS Release archive PASS, exit 0, 197.729 seconds: `/tmp/knitnote-161-release-VtfdTt/KnitNote-macOS.xcarchive`.
- iOS main app, embedded Watch app and Share extension: version 1.6.1, build 13, exact source revision above, strict signature verification PASS; no iCloud or push entitlements. Main/Share retain the expected App Group.
- macOS: version 1.6.1, build 13, exact source revision above; strict all-architecture signature verification PASS for universal x86_64/arm64. Signing team verified from signature metadata. Signed entitlements are exactly sandbox, user-selected file read/write and network client, all true.
- Diagnostic note: sandboxed codesign initially could not access trust services. Approved unsandboxed read-only verification succeeded. The Mac archive does not carry the iOS-style team entitlement; its team is present in signature metadata. No app/configuration changes were needed.
- These are local archive signatures, not App Store distribution-export verification. No IPA/PKG export or complete archive release audit performed yet.

## Live pricing observation on 2026-09-10

Read-only App Store Connect pricing inspection showed the current download price as USD 0.00 for the United States and TWD 0.00 for Taiwan. The availability summary showed 175 territories. The schedule summary displayed only the current-price row. No prices, scheduling, availability or other settings were changed. In-app purchase status/pricing remains unchecked; this is not a complete commercial-release sign-off.

## Release boundaries

The local archive step does not upload or submit. Final distribution export/audit, remaining platform/device acceptance, live commercial/build checks and exact submission authorization remain separate. Do not mark the release ready solely from the selected regression batch or an archive build exit code.

## Subsequent distribution export and full audit attempt

- Both local App Store Connect exports succeeded on 2026-09-10: iOS exit 0 in 33.970 seconds; macOS exit 0 in 23.082 seconds. Destination was `export`, not upload. Version/build auto-management remained disabled.
- Artifacts were moved (not rebuilt) into `/tmp/knitnote-161-release-VtfdTt/artifacts`, with canonical `KnitNote-iOS-Privacy.xcarchive` and `KnitNote-macOS-Privacy.xcarchive` names. Original paths above are historical.
- Exported packages: `Distribution/iOS/KnitNote.ipa` (32,880,086 bytes) and `Distribution/macOS/KnitNote.pkg` (26,928,014 bytes), relative to that artifact root. The two generated Packaging.log files were removed as required by the release workflow; binaries and export summaries were retained.
- Deterministic `provenance.json` created for source `0dd2e906f5a1726d31b67a39b83c3f5afdbb5dd6`.
- Unmodified production `release_audit.sh --archives ...` started under native arm64. It runs the complete Swift test suite before package checks. Failures observed so far include localization oracle/terminology, attachment counts in mapper/bootstrap fixtures, a stale Mac entitlement count, and semaphore timeouts in sharing tests. This is NOT a passing release audit; package/signature/localization checks later in that script have not yet run.
- Read-only diagnosis: the historical catalog oracle's allowed additional keys omit exactly the nine new `about.story.*` / `about.usage.*` keys. The Mac contract still expects six entitlements although the approved local-only source contains three. Attachment tests observed seven instead of six (and fourteen instead of twelve); root cause and focused reruns remain pending. No test expectations or production code were changed during this export step.
- Live IAP check: `com.phillon.KnitNote.lifetimeUnlock` is approved, non-consumable, available in all selected territories; current USA USD 4.99 and Taiwan TWD 150.00 match the checked-in configuration. A separate existing current-price row ends 2026-09-14 for Israel, Indonesia, Republic of the Congo and Morocco. No price settings were changed; future-price details were not fully reviewed.

Do not upload or submit based on export success while the full audit is failing.

Handoff: full audit execution session `61757` is still running at this checkpoint under a 900-second command timeout. No final suite count or exit status is available yet. Resume/poll this existing execution before launching any duplicate full run. The source clone remains clean. Sharing failures include `ShareExtensionFlowContractTests.cancelDuringProcessingSuppressesLatePublication` and `PatternShareInboxEnqueuerTests` cancellation/manifest-commit semaphore timeouts; these need isolated reproduction before attributing them to implementation defects or test load.

## Subsequent focused corrections and verification

The full audit session `61757` subsequently reached its 900-second bound and exited 124 (900.011 seconds). It did not complete all tests and never reached final exported-package validation. Do not resume that finished session or claim a complete suite count.

Read-only reproduction before changes: 45 tests in three suites failed with five issues (63.660 seconds test runtime, exit 1). Confirmed the missing nine story/usage catalog keys, obsolete Mac entitlement count, and German/French/Norwegian usage-copy terminology mismatches.

Corrections in the original worktree (not yet committed):

- Added exactly the nine approved story/usage keys to the historical catalog oracle's additional-key allowlist; preserved historical-entry comparisons and full localization validation.
- Replaced the obsolete six-entitlement count with an exact three-key local-only security allowlist; preserved checks that all three values are true.
- Changed only the usage paragraph's pattern terminology: German Strickmuster, French patrons, Norwegian mønstre. Updated the corresponding review draft. No functional production code changed.
- Complete media fixture has seven attachments: project photo, yarn photo, yarn label photo, journal full image, journal thumbnail, PDF and markup. Updated obsolete six/twelve expectations to seven/fourteen and explicitly checked retention of the label-photo output. Counter-count assertions remain six. Existing source-byte/hash and reconstruction checks retained.

Fresh validation:

- Localization/terminology/release configuration/sharing filter: 146 tests in seven suites PASS, 60.021 seconds test runtime, exit 0 (85.606 seconds including build). Sharing cancellation/commit tests pass under isolated non-parallel execution without production changes; this does not establish that the previous parallel-run timeout is permanently resolved.
- Mapper/bootstrap transaction filter: 61 tests in two suites PASS, 10.859 seconds test runtime, exit 0 (38.145 seconds including build).
- Production static release audit PASS; git diff whitespace check PASS.

The 207 focused tests are not a replacement for a complete release audit. Next: establish a reliable complete test run, freeze/review the corrected candidate, rebuild/export the changed language resources, and run exported-product validation. Archives/IPA/PKG at source `0dd2e906...` are retained as historical candidates and do not include the three terminology corrections. No push, upload, submission or pricing change occurred.

## Complete test inventory coverage, sequential batches

On 2026-09-10, the corrected working tree was tested without further source edits. Native arm64 Swift Testing used `--disable-xctest --disable-sandbox --no-parallel`; the production release-audit script was not changed.

- First complete unfiltered attempt: bounded at 1,800 seconds, exit 124 (1,800.021 seconds), no recorded test failures. Because the complete run timed out, only suites with an explicit completed PASS were credited.
- `swift test list --skip-build --disable-xctest` enumerated 2,920 unique test identifiers. The first log proved 144 complete suites covering 1,626 of those identifiers.
- The continuation excluded exactly those completed suite names and ran all other identifiers. Incomplete suites and standalone tests were not excluded. Result: **1,294 tests in 203 suites PASS**, 656.527 seconds test runtime, exit 0 (658.221 seconds including launcher).
- Independent set reconciliation verified: the skip regex selected exactly the 1,626 credited identifiers; its complement was exactly 1,294. The two disjoint coverage sets cover all **2,920** listed tests. Neither log contains a recorded test failure. This is complete inventory coverage across batches, not a single uninterrupted full-run PASS.
- Read-only sampling during a quiet log interval located active work in large JSON encoding validation, not a stalled process; buffered log output later advanced. Earlier parallel-run sharing timeouts did not recur under sequential execution. No definitive claim is made that every earlier timeout had the same cause.

Evidence directory: `/tmp/knitnote-full-serial-8CJAR0`:

- `tests.log`: initial sequential run and completed-suite evidence.
- `inventory.txt`: complete listed test identifiers.
- `continuation-selection.json`: exact exclusion expression and reconciliation counts.
- `remaining.log`: successful continuation with the final test count and exit code.
- `recovery-sample.txt`: read-only diagnostic sample from the active test runner.

Remaining release work: review/freeze the corrected source candidate, regenerate archives and distribution exports containing the terminology corrections, and complete exported-product audit plus outstanding physical/store gates. The current production archive audit still invokes an unpartitioned default test run; its reliable execution strategy must be reconciled before calling that gate PASS. No new archive, upload, push, price edit or submission was performed in this test-only step.
