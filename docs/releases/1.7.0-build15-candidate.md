# 1.7.0（15）本機候選 — 暫停，尚不可上傳

2026-09-13。使用者授權本機候選準備，未授權本次上傳或送審。

## 固定來源與產物

- 本機提交：`f0d96a9f289f9c2df712d59abafe464f97f875c8`。
- 原工作分支：`docs/cross-device-sync-design`；僅提交使用資格功能、測試、驗證紀錄與 build 15 設定共 15 個檔案，未推送。
- 乾淨 detached clone：`/private/tmp/knitnote-170-build15.o1v9hN/source`。
- 根目錄：`/private/tmp/knitnote-170-build15.o1v9hN`。
- 封存：`KnitNote-iOS.xcarchive`；xcodebuild 退出 0。
- 匯出：`Distribution/iOS/KnitNote.ipa`；退出 0、EXPORT SUCCEEDED。
- IPA SHA-256：`da92d40d29f12bd5926bbec728421ffe4606ac20ebf4ce5f53b0f4a7cc2eb068`。
- 匯出採既有 ExportOptions（destination=export），未執行 upload 或更新 provisioning。
- 產生的 Packaging.log 未讀取，已移除；保留 IPA、匯出摘要與其他驗證日誌。

## 通過的檢查

- 線上 TestFlight 在準備時最新為 1.7.0（14）；因此新候選採 build 15。
- 固定副本靜態發布檢查通過。
- 版本、發布稽核、語系及相關測試：231 項、8 組通過，1158.714 秒。
  日誌 `/tmp/knitnote-15-preflight-final.log`。
  較早測試因編譯時尚在更新版本測試來源中止，不採用。
- Archive／IPA 的主程式、Watch、Share 六個產品皆讀回 1.7.0（15）與完整來源 SHA。
- 在可存取系統信任資料的環境執行 codesign --verify --deep --strict，六個產品皆成功。
  受限環境首次信任檢查失敗，不採用該次結果；沒有修改信任設定。
- IPA 主程式權限讀回正確 team、App Group、beta-reports-active=true、get-task-allow=false。
- 實際 IPA 中的 13 語系各有 12 個新增 access 文案鍵。

## 阻擋：正常啟動第二次失敗

- 舊 iPhone 模擬器複測未能進入設定頁，顯示資料庫不可用與匯入提示。
- 為排除舊環境，使用本次獨立建立的 `KnitNote-Access-QA-iPhone`
  （2AFE640E-A637-4346-9E95-DAEA169118E4），帶 App Group 權限的 Debug 產品，
  相同來源／build 15，連續執行設定頁 UI 測試兩次。
- `iphone-clean-ui.xcresult` 明確讀回：Repetition 1 Passed、Repetition 2 Failed。
  第二次在等待「設定」入口時失敗，未執行恢復購買、未新增作品、未刪除資料。
- 失敗畫面與 UI hierarchy 已保存在 `iphone-repeat-evidence`。
- 因獨立環境也出現第二次啟動失敗，不能再僅以「舊模擬器資料問題」排除。
  根因尚未確定，可能涉及測試環境、啟動／持久化流程；不能宣稱實機必然受影響。
- 暫停候選放行與後續 iPad 複測，待診斷後再決定是否需要新來源及重新封存。

## 2026-09-13 啟動問題修正與本機驗證

- 根因：首次空資料庫載入的織圖收件匣復原建立 `Patterns/Assets` 空工作資料夾，
  尚未有使用者寫入，因此沒有 `projects-v1.json`。下一次啟動的備份復原看到 live root
  就要求有效 archive，轉成 `rollbackFailed`／`unreadableArchive`，阻擋正常入口。
- 失敗測試的系統日誌確認兩次啟動的 App Group lookup 均成功，並非缺少 App Group。
- 與 build 14 的來源比較，相關備份／載入／收件匣邏輯無差異；尚未以 build 14 重跑此案例。
- 使用者已授權修正。只允許已知且全空的啟動資料夾，且 backup work root 必須不存在；
  不建立空 archive、不清理復原證據。檔案、未知資料夾、symlink、備份痕跡保持嚴格保護。
- TDD：`/private/tmp/knitnote-empty-launch-red2.log` 的兩個測試先失敗，
  第二次載入為 unreadableArchive、直接復原為 rollbackFailed。
- 首輪修正：`/private/tmp/knitnote-empty-launch-green.log`，93 項測試通過。
- 唯讀獨立審查未發現重要問題；依建議補上空 work root／dangling symlink 的保護測試。
- 最終核心：`/private/tmp/knitnote-empty-launch-final-core.log`，110 項／2 組通過，退出 0。
  包含空資料庫重啟後首次新增並持久化、備份復原、織圖收件匣、損毀／未知內容／symlink 保護。
- 原失敗 iPhone 模擬器保留資料，修正後設定頁 UI 連續 3 次全部通過；
  `/private/tmp/knitnote-empty-launch-iphone-fixed.xcresult`。
- iPad 模擬器使用同一 Debug 產品複測，連續 3 次全部通過、退出 0，結果保存在
  `/private/tmp/knitnote-empty-launch-ipad-fixed.xcresult`。
- 本機檢查不等於完整 Core／真機／TestFlight 驗收，不代表候選放行。
- 上述修正為工作樹未提交內容，**本文件頂端 f0d96a9 的 Archive／IPA 不含修正，不可拿來上傳**。
  必須固定新來源並重新封存匯出後，才可能成為後續驗收候選。

## 發布邊界（維持不變）

未修改商店草稿，仍選 build 14；未上傳 TestFlight、未送審、未發布。
原 build 14 固定候選副本未更動。未清除任何既有模擬器或實機資料。
這不是完整雙平台發布稽核，也不是完整 Core 或商業實機驗收全部通過。
