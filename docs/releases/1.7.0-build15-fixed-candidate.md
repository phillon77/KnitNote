# 1.7.0（15）修正後本機 iOS 候選

2026-09-13。只完成本機封存、匯出及以下檢查；未上傳、未送審、未發布。

## 唯一來源與產物

- 本機提交：`2f6ebcbbdc0276d79b240802193cf66c27de6ee1`，未推送。
- 此提交只含啟動修正與兩個測試檔案；其他工作樹變更未納入。
- 乾淨 detached clone：`/private/tmp/knitnote-170-build15-fixed.nuOknt/source`。
  封存前後 git status 均空白。
- 根目錄：`/private/tmp/knitnote-170-build15-fixed.nuOknt`。
- Archive：`KnitNote-iOS.xcarchive`，archive 退出 0。
- IPA：`Distribution/iOS/KnitNote.ipa`，export 退出 0、EXPORT SUCCEEDED。
- IPA SHA-256：`b7715b089f4f549cb30115f6323561f7a7badecf3301e4274b5a9abd3255bbdd`。
- 匯出使用既有 ExportOptions，destination=export、manageAppVersionAndBuildNumber=false；
  未執行 upload 或允許 provisioning 更新。Packaging.log 未讀取。
- 舊 `knitnote-170-build15.o1v9hN` 候選不含本次修正，保留但不得用於後續上傳。

## 驗證證據

- 封存前重新執行 110 項相關測試，退出 0：
  `/private/tmp/knitnote-fixed-candidate-precheck.log`。
- 同一修正內容的 iPhone／iPad 模擬器各連續啟動 3 次通過：
  `/private/tmp/knitnote-empty-launch-iphone-fixed.xcresult`、
  `/private/tmp/knitnote-empty-launch-ipad-fixed.xcresult`。
- 乾淨副本 `release_audit.sh --static-only`：STATIC RELEASE AUDIT PASS。
- Archive 與解壓 IPA 的主程式／Watch／Share 共六個產品均讀回
  `1.7.0`、`15`、完整來源 SHA `2f6ebcbbdc0276d79b240802193cf66c27de6ee1`。
- 六個產品 `codesign --verify --deep --strict` 均退出 0（可存取系統信任資料的環境）。
- IPA 主程式 application identifier、team、App Group 正確；
  beta-reports-active=true、get-task-allow=false。
- IPA 的 13 語系各包含 12 個 access 文案鍵；繁中 access.title 為「使用資格」。
  首次讀取誤用不存在的 access.status.title，已改用實際文案鍵核對，非產品缺字。
- 靜態、封存、匯出日誌分別位於根目錄 static.log、archive.log、export.log。

## 完整 Core 補驗

- 本次新增全量 Core 檢查：2,963 項中 3 項分享流程等待逾時失敗，退出 1；
  獨立程序 14 項相關測試重跑 10 輪皆通過，但不能取代完整失敗結果。
  原始失敗保留，詳見 `1.7.0-build15-full-regression.md`。
- 後續同一乾淨來源執行 `swift test --no-parallel`：2,963 項／216 組全部通過，
  2775.544 秒、退出 0，原 3 個逾時案例均通過；無 filter、無程式或 timeout 修改。
- 最終來源與 IPA 雜湊再次核對一致。完整 Core 非並行回歸關卡已通過。

## 尚未完成的關卡

- 這不是完整雙平台發布稽核；商業狀態、真機、Watch／Share 全路徑驗收未全數完成。
- 修正後候選尚未上傳 TestFlight，也尚未在實體 iPhone／iPad 安裝驗收。
- 上傳前需重新確認線上 build 15 未占用，並取得此候選的明確上傳授權。
- 本次未更動 App Store Connect；不以舊頁面狀態當作目前線上狀態證據。
