# 1.7.0（15）修正候選完整 Core 回歸

2026-09-13。來源 `2f6ebcbbdc0276d79b240802193cf66c27de6ee1`。
測試在 `/private/tmp/knitnote-170-build15-fixed.nuOknt/source` 的乾淨副本執行。
本次僅檢查，未修改候選程式、未上傳、未送審。

## 最新完整執行：非並行全數通過

- 使用者批准後執行 `swift test --no-parallel`，無 filter、無略過案例。
- 日誌：`/private/tmp/knitnote-170-build15-fixed.nuOknt/full-core-serial.log`。
- 最終：2,963 項／216 組全數通過，2775.544 秒，程序退出 0。
- 原 3 個逾時案例分別在 0.001、0.004、0.059 秒通過；新安裝再次啟動及首次作品保存測試亦通過。
- 未修改產品或測試程式、未延長 timeout。這支持並行測試排程干擾的判斷，
  不代表已修正預設並行測試的穩定性；下列原失敗紀錄完整保留。
- 過程曾因長時間沒有新完成紀錄進行一次唯讀程序取樣：
  `serial-progress-sample.txt` 顯示同步復原流程正在執行實際驗證運算。
  該耗時案例後續正常通過，非等待鎖或中止測試。
- 最後讀回來源仍為 `2f6ebcbbdc0276d79b240802193cf66c27de6ee1`，git status 空白；
  IPA SHA-256 仍為 `b7715b089f4f549cb30115f6323561f7a7badecf3301e4274b5a9abd3255bbdd`。

## 先前完整執行：並行未通過

- 命令：`swift test`（無 filter，預設並行）。
- 日誌：`/private/tmp/knitnote-170-build15-fixed.nuOknt/full-core.log`。
- 最終：2,963 項／216 組，2,960 項通過、3 項失敗，共 10 個 issue；
  測試執行 1271.340 秒（約 21 分鐘，不含編譯），程序退出 1。
- ReleaseAuditLocalizationTests 全組通過；完整測試仍因下列分享案例失敗而不通過。
- 已出現 3 個失敗案例，不得將本次執行宣稱為全數通過：
  - ShareExtensionFlowContractTests.cancelDuringProcessingSuppressesLatePublication：
    processingStarted、callbackFinished 的 10 秒等待逾時。
  - PatternShareInboxEnqueuerTests.cancellationDuringCandidateCopyPublishesNothingAndCleansEveryArtifact：
    copyStarted、operationFinished 的 2 秒等待逾時，未取得預期取消結果。
  - PatternShareInboxEnqueuerTests.manifestCommitWinsAtomicallyOverConcurrentCancellation：
    背景工作的等待逾時及依賴結果斷言失敗。

## 唯讀診斷

- 這些測試使用 DispatchQueue.global 與阻塞式 semaphore 等待。
  本次啟動修正未修改這些測試或分享流程的產品程式。
- 同一個已編譯 test bundle 在獨立程序執行兩個相關 suite：14 項全部通過。
  日誌 `share-isolated-diagnostic2.log`（位於候選根目錄）。
- 再獨立重跑 10 輪，每輪 14 項皆通過，每輪約 0.11 秒：`share-isolated-repeat10.log`。
  原完整測試仍同時運作，因此結果支持同一程序內大量並行測試造成排程干擾的假設；
  尚未證明確切根因，也不取代失敗的完整測試結果。
- 完整測試結束後，以標準 `swift test --skip-build --no-parallel --filter
  'ShareExtensionFlowContractTests|PatternShareInboxEnqueuerTests'` 複測，14 項／2 組通過，
  0.114 秒，退出 0；日誌 `share-standard-serial.log`。
- 測試後固定副本仍乾淨；IPA SHA-256 仍為
  `b7715b089f4f549cb30115f6323561f7a7badecf3301e4274b5a9abd3255bbdd`，沒有重新匯出或變更候選。
- 獨立程序使用 SwiftPM 的 swiftpm-testing-helper 載入相同 test bundle，
  設定 Xcode MacOSX Developer/Library/Frameworks 為 DYLD_FRAMEWORK_PATH，
  filter 為 ShareExtensionFlowContractTests|PatternShareInboxEnqueuerTests。
  初次直接執行 bundle 及未設定 framework 的 helper 不可執行，不計入產品測試結果。

## 關卡

完整 Core 非並行回歸關卡已通過；預設並行執行的逾時保留為測試穩定性議題。
尚未執行 TestFlight 上傳或送審。上傳前仍須核對線上 build 狀態並取得此候選的明確授權。
修正後 TestFlight／真機／商業全路徑驗收仍未完成，不能宣稱可正式發布。
