# 1.7.0（14）本機版本調整驗證

使用者確認本機 shipping targets 統一使用 1.7.0（14），此次只建置
iOS 產品及其嵌入項目，不動線上 macOS 審查。

版本設定／測試提交：`0865eb4d22def070160c409a370473e8be0239df`。
測試與建置於提交前對相同設定執行；之後只提交這八個設定／測試檔，
沒有修改功能程式碼。既有未提交驗收紀錄、翻譯草稿與其他檔案均保留。

## 驗證結果

- 基準：`swift test --no-parallel --filter 'ReleaseCandidateIdentityTests|ReleaseConfigurationContractTests|WatchPackagingContractTests'`
  通過 52 項／3 組，88.508 秒，退出 0。
- RED：候選版本契約更新後，`projectUsesProductionIdentifiersVersionAndTeam`
  對舊設定出現預期的版本號／建置號兩項失敗，退出 1。
- GREEN：上述三組加上 `archiveAuditRejectsPrevious` 與
  `archiveAuditAcceptsThirteenMatching`，通過 55 項／4 組，108.747 秒，退出 0。
  包含 Xcode 實際設定讀回、舊版封存拒絕與新版本測試封存接受。
- 候選建立／清理：`supportedCandidateCreatorExportsBothArchivesBeforeProvenanceAuditAndPublication`
  與 `creatorFixturePublishesPrivateCandidateWithoutRawPackagingLogsAndCleansRaceStaging`
  通過 2 項／1 組，5.988 秒，退出 0；只用測試替身，不是真實簽署封存。
- `bash AppStore/Verification/release_audit.sh --static-only`：退出 0，
  `STATIC RELEASE AUDIT: PASS`。
- `xcodebuild build -quiet -project KnitNote.xcodeproj -scheme KnitNote -configuration Release -destination 'generic/platform=iOS' -derivedDataPath /private/tmp/knitnote-170-derived CODE_SIGNING_ALLOWED=NO`：退出 0。
- 讀回建置產品 `KnitNote.app/Info.plist`、
  `KnitNote.app/Watch/KnitNoteWatch.app/Info.plist` 及
  `KnitNote.app/PlugIns/KnitNoteShare.appex/Info.plist`：三者皆為 1.7.0（14）。
- `git diff --check` 通過；Sources、功能 UI、本地化資源與正式 metadata 未修改。

暫存日誌位於 `/private/tmp/`：`knitnote-170-version-baseline.log`、
`knitnote-170-version-red.log`、`knitnote-170-version-green.log`、
`knitnote-170-creator.log`、`knitnote-170-static.log`、`knitnote-170-ios-build.log`。

## 非發布完成

這是版本設定及未簽署 Release 建置驗證，不是正式發行封存。
未執行完整 Core／App 回歸、簽署、安裝、上傳、推送、商店資料修改或送審。
未簽署產品於提交前建立，不宣稱內嵌來源版本等於上述提交；
正式候選仍須從凍結來源重新建置並驗證來源識別。

正式 metadata 與其驗證規則仍保存 1.6.1 的「資料優化」；靜態檢查通過
不表示 1.7.0 商店文案已就緒。新版 13 語系內容仍在獨立草稿中，
需要審閱後才套用。最終版本／建置號、候選驗收及上傳授權須另行確認。
