# 1.7.0 本機更新文案驗證

依使用者確認，將日記分享 13 語系文案套用至本機 What's New。
適用 iPhone／iPad，不適用 macOS；線上 App Store Connect 未更動。

## 審閱與變更

- 逐段核對兩種比例（4:5／9:16）、分享前調整且不改原日記、儲存照片、
  複製文字及系統分享。未加入自動發文、iCloud 或所有社群實測的宣稱。
- 翻譯為助理審閱結果，非另經母語審稿者認證。
- 沿用既有單行欄位格式，換行與項目符號轉為空格及「•」，文字完整保留。
- 13 份 metadata 各只替換 What's New 一行。其他欄位雜湊保留測試通過。
- 本機驗證契約改為 1.7.0 文案，保留其他欄位與禁止宣稱檢查，
  增加錯誤比例與舊「資料優化」文案拒絕案例。

## 本次驗證

- 基準：`python3 -m unittest AppStore.Verification.metadata_check_test`，30 項通過。
- 初次測試編輯少了迴圈，出現 IndentationError；修正測試後才取得有效 RED。
- 有效 RED：30 項中 130 個子案例失敗，原因是舊文案與 1.6.1 驗證契約
  不符合新要求。日誌 `/private/tmp/knitnote-170-metadata-red-fixed.log`。
- GREEN：同一套 30 項全部通過，1.179 秒。
- `python3 AppStore/Verification/metadata_check.py AppStore/Metadata`：PASS。
- `bash AppStore/Verification/release_audit.sh --static-only`：退出 0，
  `STATIC RELEASE AUDIT: PASS`；日誌 `/private/tmp/knitnote-170-metadata-static.log`。
- `git diff --check` 通過；本次未修改功能程式碼、Xcode 設定或版本號。

## 保留的發布關卡

本次沒有簽署、建置、裝置安裝、推送、上傳或送審；不構成完整候選驗收。
其他商店欄位仍需逐欄核對線上值，尤其歷史副標題不可直接覆蓋。
詳見 [平台套用限制](../../AppStore/MetadataScope.md)。
