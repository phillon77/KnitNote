# KnitNote 1.7.1 (16) release progress — 2026-09-18

## Authorized scope
User goal: submit KnitNote 1.7.1 and push source. User explicitly selected iOS/iPadOS (Watch/Share included) AND macOS. Use Codex in-app browser for ASC, as explicitly requested. No main-branch merge was requested; prepare/push feature/knitnote-short-rows to origin https://github.com/phillon77/KnitNote.git, with exact source SHA reported before push.

## Current source
Worktree `.worktrees/knitnote-short-rows`, branch `feature/knitnote-short-rows`, base `8fe9da8fbcc36b9dd53b6fd674cefbccc3b4efb1`. Changes remain uncommitted. Includes prior macOS journal sharing commit 6f63818 by ancestry, stitch dictionary and tutorial links, and new shared ShortRowKit.
Main/Watch/Share are now 1.7.1 (16). Release audit, creator staging names and version identity tests aligned. Legacy data fixture schema 14 was preserved.

## Verification
- ShortRowKit 9 tests PASS: `/tmp/knitnote-171-shortrows.log`.
- Metadata 30 tests PASS: `/tmp/knitnote-171-metadata-tests.log`. All 13 local What's New fields now describe shoulder short rows + stitch dictionary/tutorials and disclose en/zh-Hant short-row language scope. Other metadata fields preserved.
- Static release audit PASS after metadata changes: `/tmp/knitnote-171-static-final.log`.
- First full core run used incompatible `/Library/Frameworks/Python.framework/Versions/3.9/bin/python3`; its audit fixtures failed before actual checks. Explicitly terminated that failed run (exec session 20985 exit143), not treated as passing.
- Additional root causes from first run: Watch packaging assertion assumed Watch was first dependency; adding ShortRowKit invalidated only that string layout. Test now still asserts exact Watch embedding/platform block without requiring first position. Historical catalog oracle permitted additions omitted exactly 83 stitchDictionary keys introduced by the existing dictionary implementation; added those exact keys, preserving historical oracle entries and completeness checks.
- Core rerun uses `PATH=/usr/bin:/bin:/usr/sbin:/sbin:/opt/homebrew/bin swift test --scratch-path /tmp/knitnote-171-core --no-parallel`; log `/tmp/knitnote-171-core-rerun.log`; live exec session **24057**, PID17566 / test child17676 at latest check. No errors observed yet; NOT finished.
- macOS unsigned Release build log `/tmp/knitnote-171-mac.log`, derived `/tmp/knitnote-171-mac`; live exec session **57442**, xcodebuild PID17973 at latest check. Active arm64/x86_64 compiler CPU confirmed; NOT finished.
- Do not restart either live process merely because logs are briefly unchanged. Poll existing sessions.

## App Store Connect
App 6793023054. Live original versions: iOS1.7.0 and macOS1.6.1 both Available for Release. Both new1.7.1 drafts created successfully; Prepare for Submission, no selected build.
User completed in-app-browser login. CUA `knTab`, tab id1, currently macOS inflight URL. `knTab.playwright` is supported; use DOM snapshot and grounded locators. Safari no longer used per user instruction.
macOS shared review notes saved with new feature test steps and unchanged trial/IAP/no-cloud scope. macOS all13 locale notes have been entered and saved; reload/readback verified zh-Hant,en-US,zh-Hans,fr-FR,nb-NO,fi-FI,ko-KR,nl-NL. A rapid locale loop skipped actual persistence for de-DE,ja-JP,sv-SE,da-DK,el-GR; these five were separately re-entered and saved successfully but still need final reload/readback. Current locale el-GR. iOS notes/review still need update.
Use separate focus/load and fill/save phases, or prove locale load complete; a changed language button alone may precede field loading. Never assume text entry proves persistence.
Existing auto-release setting was inherited and unchanged. Existing screenshots inherited. Contact fields appear redacted in browser output; do not overwrite.

## Remaining
1. Finish/review core and macOS build; fix genuine remaining blockers without weakening checks.
2. Complete iOS 13locale What's New + review notes, verify all macOS notes after reload, inspect pricing/IAP/privacy/screenshots.
3. Freeze exact source with local commit. Push authorized feature branch (state SHA/boundaries, do not touch main or include unrelated files). Verify remote SHA.
4. Archive/export both platforms from exact commit, verify signatures/versions/source/artifact provenance, upload/process/select builds.
5. Reconcile candidate physical acceptance: earlier user iPhone checks cover shortrow behavior on1.7.0(15); document source delta, verify1.7.1 release surfaces. iPad/macOS new tool runtime acceptance remains missing.
6. Export compliance needs concrete user declaration per release skill when actual form is known; prior blanket submit authority is retained.
7. Submit both platforms under existing explicit authorization; record submission IDs and Waiting for Review. Only then complete goal.

## Continued verification checkpoint
- macOS unsigned Release build completed successfully, exec57442 exit0. Runtime launched from `/tmp/knitnote-171-mac/Build/Products/Release/KnitNote.app`; Settings displays1.7.1(Build16). Opened Shoulder Short Rows, loaded sample and visually inspected input/result, diagram and all row instructions. AX confirms6rows/3turns/2cm and18/12/6 outgoing,24stitch finishing row. This is agent-observed real Mac runtime QA, not user-confirmed or App Store signing validation.
- iOS and macOS all13 What's New localizations now saved and read back through separate locale selection; both review notes updated. Five initially missing macOS locales were re-saved and then all five verified after reload. iOS exact per-locale field equality verified for all13; inherited release method remains automatic after approval.
- Live commerce: download price US$0.00/NT$0.00;175territories; Lifetime Unlock non-consumable com.phillon.KnitNote.lifetimeUnlock approved, all countries selected, current US$4.99/NT$150. No pricing changes.
- Live KnitNote privacy label remains No Data Collected; policy https://phillon77.github.io/KnitNote/privacy.html. No change to declaration; new tool has no ad SDK.
- iOS signed Release device build started, exec session **9943**, derived `/tmp/knitnote-171-device`, log `/tmp/knitnote-171-device.log`. Still active at checkpoint; no errors observed. Physical iPhone17ProMax and iPadAir5 are available(paired), not yet updated to this candidate.
- Core rerun exec24057 still live, progressed into ReleaseConfigurationContractTests without failure markers. Continue polling existing session; no restart.
- In-app browser tab1 currently KnitNote privacy page. `knNotes` contains all13 release-note strings; `knReview` has common review notes. Helper `saveExact171(locale)` and `selectExact171(from,to)` accept explicit locale arguments. Do not rely on a helper's assignment to an external REPL variable to persist across tool calls; this caused one transient iOS simplified-Chinese value mismatch, corrected and verified before this checkpoint.

## Dependency repair and device checkpoint
- iOS signed Release build completed with BUILD SUCCEEDED, exec9943 exit0, log `/tmp/knitnote-171-device.log`.
- Core rerun is still active. StoreScreenshotFixturesTests failed with48 issues because screenshot Python could not import pinned Pillow. Diagnosis: installed Pillow11.3.0 contained x86_64 `_imaging`, while Xcode Python3.9 runs arm64. Reinstalled the same pinned version using `/usr/bin/python3 -m pip install --user --force-reinstall --no-cache-dir --only-binary=:all: Pillow==11.3.0`; all required PIL modules import successfully and report11.3.0. No product code or test assertions changed for this environment fault.
- Scoped screenshot retry running with separate scratch path `/tmp/knitnote-171-screenshot-retry`, log `/tmp/knitnote-171-screenshot-retry.log`, exec47298. Full rerun24057 continues to collect unrelated results; its screenshot failures must not be presented as a passing complete run.
- iPhone install attempt failed before installation due CoreDevice connection reset. Asked user to unlock/connect iPhone and iPad; awaiting reply. Existing device data remains unchanged by that failed attempt.
- Scoped StoreScreenshotFixturesTests rerun completed:20 tests passed in8.673s, log `/tmp/knitnote-171-screenshot-retry.log`. Confirms Pillow architecture repair resolves all previously failing screenshot fixtures. Full core run still executing owned interruption matrix tests; final result pending.

## Continuation live-state check
- Previous goal turn made progress: repaired Pillow architecture and completed20 screenshot fixture tests.
- Core exec24057 is confirmed live by poll and process state (swiftpm-testing-helper PID17676 actively using CPU); log has advanced into selector recovery conflict cases. This is a verified wait, not a stopped run. No restart.
- iPad installation also attempted and failed before installation with CoreDevice4000 / connection reset by peer. Both iPhone and iPad require renewed physical connectivity; user connection question remains pending. No app-data removal or signing changes performed.
- Short process sample `/tmp/knitnote-171-test-sample.txt` confirms the live core runner is executing mediaDeletionFIFOCompletesOwnedAndAuthenticatedRecovery → SyncAccountRecoveryTransaction.cleanupLocked → recoveryEntries. Log buffering can lag actual execution; unchanged tail is not a hang or terminal state. Sampling did not interrupt the process.

## Pre-freeze verification result
- Full core rerun24057 completed exit1 after2300.026s:2996 tests /221 suites,48 issues. Every failure is in StoreScreenshotFixturesTests, all attributable to the previously diagnosed Pillow architecture mismatch; no failures in other suites. The repaired environment's scoped20-test screenshot rerun completed exit0. Do not label the earlier whole run PASS; the immutable candidate archive audit will execute the whole suite again.
- Both target Release builds,9 ShortRowKit tests,30 metadata tests, static release audit, and diff whitespace check passed. Feature source has not changed since these checks.
- Freeze source now for archive provenance. Upload/submission remains pending archive audit and physical acceptance. All13 locale notes and review notes are persisted on both ASC drafts; no build is selected yet.
