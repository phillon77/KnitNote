# 跨設備同步第三階段交接

日期：2026-09-05

版本維持 **1.7.0（13）**。程式與測試修正提交：`7eb37b92cdf43f36a6b89813b80f982bbebab0fd`。

第三階段的有界基礎實作與程式審查已完成；**不代表完整跨設備同步、正式 App 接線、全套測試或送審準備已完成**。未合併、推送、封存、上傳或送審。

## 完成範圍

- 既有資料匯出／移轉、回滾證明與移轉收據。
- 已刪除資料的獨立保留、復原與永久防復活標記。
- 帳號獨占儲存、待同步內容加密備份、驗證後清理、原帳號精確還原。
- 帳號切換協調元件：先停止舊寫入，具體備份／清理，再開啟新帳號；實際初次抓取與合併確認前阻擋送出。

五個父任務各自通過審查。帳號復原子計畫五個任務及整合審查通過；第三階段總審查另找出三個跨元件問題，經單一集中修正與針對性複查後全部解決，沒有新的 Critical／Important。

## 總審查修正

1. 統一新帳號日誌為 `working-set/SyncMetadata/pending.json`，讓移轉、實際帳號流程、備份及還原使用同一位置。舊的非標準日誌配置明確拒絕，沒有搬檔或改寫不可變附件 URL。
2. 移轉在替換正式資料及提交收據前建立可驗證的刪除保留群組。已驗證無媒體作品與六個計數器的完整刪除，以及其他仍存活作品的已驗證圖片；第 29 天復原、回滾、實際待同步版本選取皆有回歸。缺少或不明的媒體／嵌入資料刪除依據仍提前拒絕。
3. 實際新增標註再次匯出時，沿用原本唯一相容角色、識別與前驅，不再產生第二個角色。兩類標註皆覆蓋新增、匯出、移轉、後續編輯、刪除及復原。

## 驗證證據

各次結果綁定其當時原始碼，不可相加宣稱一次完整測試。

| 驗證 | 結果與界線 |
| --- | --- |
| 集中修正相關 Core | 155 項／9 組通過；在最後圖片支援補修之前 |
| 實際同步原始碼的無介面整合 | 103 項／3 組通過；使用原始 Core／CloudSync，非替代實作 |
| 最後圖片／保留資料補修 | 57 項／4 組通過，綁定最終程式修正 |
| 最終實際 Xcode 專案 | macOS `build-for-testing` 成功；停用簽名，未執行 App 宿主 |
| 較早完整 Core 嘗試 | 在 `2fcd591` 達 15 分鐘上限後停止，2,213 個通過結果行，沒有最終總數；不可宣稱全套通過 |
| 完整嘗試的三項失敗 | Unix socket 建立及 Release／Debug 建置設定讀取受沙箱限制；精確的 6 項環境診斷重跑通過，未修改產品設定掩蓋錯誤 |

紀錄保存在 `/tmp/plan3-final-fix-core-affected.log`、`/tmp/plan3-final-fix-runtime-affected.log`、`/tmp/plan3-final-fix-supporting-media-green.log`、`/tmp/plan3-final-fix-build-for-testing-final.log`、`/tmp/plan3-final-full-swift.log` 與 `/tmp/plan3-final-environment-focused.log`。

既有 SwiftPM 快取、CoreSimulator 服務及 AppIntents 警告仍在，不宣稱輸出無警告。

較早一次選到零項測試的 Xcode 執行曾啟動 App 宿主。後來確認其初始化可能建立正式資料服務；沒有確認它是否觸及或改變正式資料，因此不能聲稱該次啟動完全無副作用。之後沒有再啟動宿主，也沒有額外探查使用者資料／Keychain。正式宿主測試需先建立可靠隔離。

## 必須保留的界線

- 加密備份只保留實際待同步內容與必要依賴，最多 100,000,000 bytes，含編碼負擔。純遠端已確認刪除歷史納入清理清單，但不擴張成備份內容；回到原帳號後的雲端重取仍需正式接線。
- 還原遇到不相符或部分寫入的檔案會拒絕並保留加密副本，未提供任意部分檔案的自動修補。
- 未解交易、舊日誌、舊提醒轉換、既有雙角色標註、缺少來源與超額內容仍有明確阻擋條件；不以推測遷移、假 ACK 或丟棄資料解除。
- 生產 UI 停止／隱藏／凍結、正式 domain committer 與日常 canonical checkpoint 尚未接入。
- 墓碑附件的實際下載保存、嵌入移除資料依據及待送永久標記的傳輸消費等既有 shipping gates 仍需完成。
- 真實 CloudKit／Keychain、程序終止恢復、App 宿主及 iPhone／iPad／Mac／Watch／分享延伸功能的實機驗收未完成。不宣稱記憶體清零。

## 下一步

進入第四階段，先將精確 canonical 狀態接入日常集中提交／發布／恢復流程，驗證「移轉 → 編輯 → 異常終止 → 重開 → 繼續同步」；再接正式 domain／UI 帳號生命週期，補齊上述相容性與傳輸阻擋條件。完成安全隔離的完整驗證與實機矩陣後，才重新評估推送、建置上傳與送審。

工作分支與所有未合併的 SDD 計畫／裁定／報告保留，不刪除工作樹。
